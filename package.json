{
  "name": "burger-plus-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Burger Plus — musteri, personel ve super admin uygulamalari tek catida (Vercel)",
  "scripts": {
    "install:all": "cd apps/musteri && npm install && cd ../personel && npm install && cd ../superadmin && npm install",
    "build:musteri": "cd apps/musteri && npm install && npm run build",
    "build:personel": "cd apps/personel && npm install && npm run build",
    "build:superadmin": "cd apps/superadmin && npm install && npm run build -- --base=/super-admin/",
    "build": "npm run build:musteri && npm run build:personel && npm run build:superadmin && node birlestir.js",
    "dev:musteri": "cd apps/musteri && npm run dev",
    "dev:personel": "cd apps/personel && npm run dev",
    "dev:superadmin": "cd apps/superadmin && npm run dev"
  }
}
