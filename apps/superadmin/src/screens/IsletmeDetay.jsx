import { useEffect, useState } from "react";
import { abonelikGuncelle, abonelikOlustur, erisimTokeniOlustur, isletmeDurumuDegistir, isletmeGetir, isletmeGuncelle, isletmeSil, personelPanelineGit } from "../lib/superApi";
import { Basari, DurumRozeti, Hata, Metrik, para, sayi, Yukleme } from "../components/Ui";

export default function IsletmeDetay({ id, kapat, degisti }) {
  const [veri, setVeri] = useState(null), [form, setForm] = useState(null), [abonelik, setAbonelik] = useState(null);
  const [hata, setHata] = useState(""), [mesaj, setMesaj] = useState(""), [islemde, setIslemde] = useState(false), [onay, setOnay] = useState("");
  const yukle = () => isletmeGetir(id).then((sonuc) => {
    setVeri(sonuc); const i = sonuc.isletme;
    setForm({ ad: i.ad, slug: i.slug, konsept: i.konsept, iletisimEmail: i.iletisimEmail || "", iletisimTelefon: i.iletisimTelefon || "", adres: i.adres || "", notlar: i.notlar || "" });
    setAbonelik(i.abonelik ? { ...i.abonelik, baslangicTarihi: String(i.abonelik.baslangicTarihi).slice(0, 10), bitisTarihi: i.abonelik.bitisTarihi ? String(i.abonelik.bitisTarihi).slice(0, 10) : "" } : { plan: "baslangic", aylikUcret: 0, durum: "deneme", baslangicTarihi: new Date().toISOString().slice(0, 10), bitisTarihi: "", notlar: "" });
  }).catch((e) => setHata(e.message));
  useEffect(() => { setVeri(null); setHata(""); yukle(); }, [id]); // eslint-disable-line react-hooks/exhaustive-deps
  const calistir = async (islem, basari) => { setIslemde(true); setHata(""); setMesaj(""); try { await islem(); setMesaj(basari); await yukle(); degisti(); } catch (e) { setHata(e.message); } finally { setIslemde(false); } };
  if (!veri || !form || !abonelik) return <aside className="detay-panel"><button className="detay-kapat" onClick={kapat}>×</button>{hata ? <Hata mesaj={hata} /> : <Yukleme />}</aside>;
  const i = veri.isletme, tema = veri.tema;
  const paneleGit = async () => { setIslemde(true); try { const sonuc = await erisimTokeniOlustur(i.id); personelPanelineGit(sonuc.token); } catch (e) { setHata(e.message); setIslemde(false); } };
  const sil = async () => {
    if (onay !== i.slug) { setHata("Silmek için işletmenin slug değerini birebir yazın."); return; }
    if (!window.confirm(`${i.ad} yumuşak silmeye alınacak. ${i.kullaniciSayisi} kullanıcı ve ${i.siparisSayisi} sipariş etkilenecek. Devam edilsin mi?`)) return;
    setIslemde(true); try { await isletmeSil(i.id, onay); degisti(); kapat(); } catch (e) { setHata(e.message); } finally { setIslemde(false); }
  };
  return <aside className="detay-panel"><button className="detay-kapat" onClick={kapat}>×</button><header className="detay-baslik"><span className="detay-logo">{i.logoUrl ? <img src={i.logoUrl} alt="" /> : i.ad.slice(0, 2).toUpperCase()}</span><div><small>{i.slug}</small><h2>{i.ad}</h2><DurumRozeti durum={i.silinmeTarihi ? "siliniyor" : i.aktif ? "aktif" : "askida"} /></div></header><Hata mesaj={hata} /><Basari mesaj={mesaj} /><section className="detay-metrikler"><Metrik baslik="Kullanıcı" deger={sayi(i.kullaniciSayisi)} /><Metrik baslik="Sipariş" deger={sayi(i.siparisSayisi)} ton="mavi" /><Metrik baslik="Toplam ciro" deger={para(i.toplamCiro)} ton="yesil" /><Metrik baslik="Personel" deger={sayi(i.personelSayisi)} ton="turkuaz" /></section><button className="panel-erisim" disabled={islemde || i.silinmeTarihi} onClick={paneleGit}>Bu işletmenin paneline git ↗</button><section className="detay-bolum"><h3>Genel bilgiler</h3><div className="form-grid"><label><span>İşletme adı</span><input value={form.ad} onChange={(e) => setForm({ ...form, ad: e.target.value })} /></label><label><span>Slug</span><input value={form.slug} onChange={(e) => setForm({ ...form, slug: e.target.value.toLowerCase() })} /></label><label><span>Konsept</span><select value={form.konsept} onChange={(e) => setForm({ ...form, konsept: e.target.value })}><option value="burger">Burger</option><option value="cafe">Cafe</option><option value="pizza">Pizza</option></select></label><label><span>İletişim e-postası</span><input type="email" value={form.iletisimEmail} onChange={(e) => setForm({ ...form, iletisimEmail: e.target.value })} /></label><label><span>Telefon</span><input value={form.iletisimTelefon} onChange={(e) => setForm({ ...form, iletisimTelefon: e.target.value })} /></label><label className="tam"><span>Adres</span><textarea value={form.adres} onChange={(e) => setForm({ ...form, adres: e.target.value })} /></label><label className="tam"><span>Notlar</span><textarea value={form.notlar} onChange={(e) => setForm({ ...form, notlar: e.target.value })} /></label></div><button disabled={islemde} onClick={() => calistir(() => isletmeGuncelle(i.id, form), "İşletme bilgileri güncellendi.")}>Bilgileri Kaydet</button></section><section className="detay-bolum"><h3>Abonelik</h3><div className="form-grid"><label><span>Plan</span><select value={abonelik.plan} onChange={(e) => setAbonelik({ ...abonelik, plan: e.target.value })}><option value="baslangic">Başlangıç</option><option value="profesyonel">Profesyonel</option><option value="kurumsal">Kurumsal</option></select></label><label><span>Aylık ücret</span><input type="number" min="0" value={abonelik.aylikUcret} onChange={(e) => setAbonelik({ ...abonelik, aylikUcret: e.target.value })} /></label><label><span>Durum</span><select value={abonelik.durum} onChange={(e) => setAbonelik({ ...abonelik, durum: e.target.value })}><option value="aktif">Aktif</option><option value="deneme">Deneme</option><option value="askida">Askıda</option><option value="iptal">İptal</option></select></label><label><span>Bitiş tarihi</span><input type="date" value={abonelik.bitisTarihi || ""} onChange={(e) => setAbonelik({ ...abonelik, bitisTarihi: e.target.value })} /></label><label className="tam"><span>Abonelik notu</span><textarea value={abonelik.notlar || ""} onChange={(e) => setAbonelik({ ...abonelik, notlar: e.target.value })} /></label></div><button disabled={islemde} onClick={() => calistir(() => abonelik.id ? abonelikGuncelle(abonelik.id, abonelik) : abonelikOlustur({ ...abonelik, isletmeId: i.id }), "Abonelik güncellendi.")}>Aboneliği Kaydet</button></section><section className="detay-bolum"><h3>Tema önizlemesi</h3><div className="tema-serit" style={{ "--isletme-accent": tema.renkler.accent, "--isletme-bg": tema.renkler.bgPrimary, "--isletme-card": tema.renkler.bgCard }}><div>{i.logoUrl ? <img src={i.logoUrl} alt="" /> : <b>{i.ad}</b>}<h4>{tema.metinler.slogan}</h4><span>{tema.metinler.kampanyaBaslik}</span></div><code>{tema.renkler.accent} · {tema.font.baslik}</code></div></section><section className="detay-bolum tehlikeli"><h3>Tehlikeli bölge</h3><p>{i.silinmeTarihi ? "Bu işletme 30 günlük yumuşak silme bekleme döneminde; kalıcı silme uygulanmadan geri yüklenebilir." : "Askıya alınan işletmenin müşteri sitesi kapanır. Silme işlemi önce 30 günlük yumuşak silme durumuna geçer."}</p><div className="tehlike-islemleri"><button disabled={islemde} onClick={() => { const soru = i.silinmeTarihi ? "İşletme yumuşak silmeden geri yüklensin mi?" : i.aktif ? "İşletme askıya alınsın mı? Müşteri sitesi kapanacaktır." : "İşletme yeniden etkinleştirilsin mi?"; if (window.confirm(soru)) calistir(() => isletmeDurumuDegistir(i.id, i.silinmeTarihi ? true : !i.aktif), i.silinmeTarihi ? "İşletme geri yüklendi." : i.aktif ? "İşletme askıya alındı." : "İşletme etkinleştirildi."); }}>{i.silinmeTarihi ? "Geri Yükle" : i.aktif ? "Askıya Al" : "Etkinleştir"}</button><input disabled={Boolean(i.silinmeTarihi)} placeholder={`Onay için ${i.slug} yazın`} value={onay} onChange={(e) => setOnay(e.target.value)} /><button className="sil" disabled={islemde || i.silinmeTarihi} onClick={sil}>Yumuşak Sil</button></div></section></aside>;
}
